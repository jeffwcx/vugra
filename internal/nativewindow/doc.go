package nativewindow

// Package nativewindow contains optional native window backends. Production
// native windows are compiled behind explicit platform/build tags so the core
// compiler, runtime, layout, and renderer stay testable without GUI processes.

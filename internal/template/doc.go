package template

// Package template parses Vugra's supported standard-HTML template subset into
// a renderer-independent AST. It recognizes Vugra's first directive set but
// does not assume browser DOM behavior.
